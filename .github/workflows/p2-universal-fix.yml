name: P2 universal stage fix (disabled)
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled; P2 workflow changes are handled by the dedicated workflow."
