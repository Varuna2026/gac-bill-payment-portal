name: P2 Warehouse Upload Docs fix
on:
  push:
    branches: [development]
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Warehouse fix workflow disabled; current UI is maintained by the active P2 UI workflow."